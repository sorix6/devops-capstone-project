apiVersion: apps/v1
kind: Deployment
metadata:
  name: production
  labels:
    app: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: production
  template:
    metadata:
      labels:
        app: production
    spec:
      containers:
      - name: production
        imagePullPolicy: Always
        image: sorix6/capstone-project
        ports:
        - containerPort: 80
  strategy:
    type: RollingUpdate